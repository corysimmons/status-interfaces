{
  "name": "status-interfaces",
  "version": "3.0.1",
  "repository": "corysimmons/status-interfaces",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "babel -w src -d dist"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "8"
          }
        }
      ]
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  }
}
